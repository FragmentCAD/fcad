# FragmentCAD Studio Tauri Backend

This directory contains the Tauri backend implementation for FragmentCAD Studio.

## Purpose
- Handles system-level interactions.
- Interfaces with `fcad-core` via IPC.
- Manages application window lifecycle.

## Development
- Use `bun` for frontend interactions.
- Rust is used for the backend logic within Tauri.
